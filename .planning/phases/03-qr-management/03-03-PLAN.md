---
phase: 03-qr-management
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/dashboard/page.tsx
  - src/app/dashboard/[id]/edit/actions.ts
  - src/components/dashboard/qr-list.tsx
  - src/components/dashboard/qr-list-row.tsx
  - src/components/dashboard/delete-button.tsx
  - src/components/qr-management/delete-dialog.tsx
autonomous: true
requirements:
  - LIST-01
  - LIST-02
  - LIST-03
  - DEL-01
  - DEL-02
  - DEL-03
  - GEN-03
  - ANLYT-01

must_haves:
  truths:
    - "Dashboard shows all user's active QR codes ordered by created_at descending"
    - "Each row displays 40px QR thumbnail, label, slug, platform badge, scan count, and edit/delete actions"
    - "Empty state shows 'No QR codes yet' with CTA linking to /dashboard/new"
    - "Clicking delete opens a confirmation dialog naming the specific QR code"
    - "Confirming delete sets is_active=false and the row disappears from the list"
    - "User can download QR as PNG named {slug}-fluxqr.png from the list row"
    - "Scan counts >= 1000 display as compact notation (e.g., 1.2k)"
  artifacts:
    - path: "src/app/dashboard/page.tsx"
      provides: "Server Component fetching QR codes and generating thumbnails"
    - path: "src/app/dashboard/[id]/edit/actions.ts"
      provides: "deleteQrCode Server Action for soft delete"
      exports: ["deleteQrCode"]
    - path: "src/components/dashboard/qr-list.tsx"
      provides: "Client Component list wrapper"
      exports: ["QrList"]
    - path: "src/components/dashboard/qr-list-row.tsx"
      provides: "Single QR code row with all data and actions"
      exports: ["QrListRow"]
    - path: "src/components/dashboard/delete-button.tsx"
      provides: "Delete button triggering confirmation dialog"
      exports: ["DeleteButton"]
    - path: "src/components/qr-management/delete-dialog.tsx"
      provides: "AlertDialog confirmation for soft delete"
      exports: ["DeleteDialog"]
  key_links:
    - from: "src/app/dashboard/page.tsx"
      to: "supabase.from('qr_codes').select"
      via: "Server Component data fetch with RLS"
      pattern: "supabase.*from.*qr_codes.*select"
    - from: "src/app/dashboard/page.tsx"
      to: "src/lib/qr-generator.ts"
      via: "generateQrDataUrl for each QR code"
      pattern: "generateQrDataUrl"
    - from: "src/components/dashboard/delete-button.tsx"
      to: "src/app/dashboard/[id]/edit/actions.ts"
      via: "deleteQrCode server action call"
      pattern: "deleteQrCode"
    - from: "src/components/dashboard/qr-list-row.tsx"
      to: "src/lib/qr-generator.ts"
      via: "downloadQrPng for PNG download"
      pattern: "downloadQrPng"
---

<objective>
Build the dashboard QR code list with data fetching, QR thumbnail generation, list rows with all actions (edit link, download, delete), empty state, and the soft-delete flow with confirmation dialog.

Purpose: The list is the central hub of the dashboard — users see all their QR codes, download PNGs, and manage lifecycle (edit/delete) from here.
Output: Working dashboard page showing QR codes with thumbnails, action buttons, and delete confirmation flow.
</objective>

<execution_context>
@/Users/enzo.figueiredo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/enzo.figueiredo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-qr-management/03-CONTEXT.md
@.planning/phases/03-qr-management/03-RESEARCH.md
@.planning/phases/03-qr-management/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs and existing codebase -->

From src/types/index.ts:
```typescript
export type Platform = 'whatsapp' | 'sms' | 'telegram'
export type QrCode = {
  id: string; user_id: string; slug: string; label: string;
  platform: Platform; contact_target: string; default_message: string | null;
  is_active: boolean; scan_count: number; created_at: string; updated_at: string;
}
```

From src/lib/qr-generator.ts (created in Plan 01):
```typescript
export async function generateQrDataUrl(slug: string): Promise<string>
export function downloadQrPng(dataUrl: string, slug: string): void
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
export function formatScanCount(n: number): string
```

From src/lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient>
```

From src/components/shared/ (created in Plan 01):
```typescript
// page-header.tsx
export function PageHeader(props: { title: string; description?: string; action?: { label: string; href: string } }): JSX.Element
// platform-badge.tsx
export function PlatformBadge(props: { platform: Platform }): JSX.Element
// empty-state.tsx
export function EmptyState(): JSX.Element
```

From src/components/ui/ (shadcn):
- alert-dialog.tsx: AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, AlertDialogTrigger
- button.tsx: Button
- badge.tsx: Badge
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create delete Server Action and delete dialog component</name>
  <files>
    src/app/dashboard/[id]/edit/actions.ts,
    src/components/qr-management/delete-dialog.tsx
  </files>
  <action>
**src/app/dashboard/[id]/edit/actions.ts** — `'use server'` directive:
- Export `deleteQrCode(id: string): Promise<{ error?: string }>`:
  1. Call `createClient()`, then `supabase.auth.getUser()`. If no user, `redirect('/login')`.
  2. Update: `supabase.from('qr_codes').update({ is_active: false }).eq('id', id).eq('user_id', user.id)`.
  3. Call `revalidatePath('/dashboard')`.
  4. Return `{}` on success.

NOTE: This file will also contain `updateQrCode` in Plan 04. For now, only create `deleteQrCode`. The file must have `'use server'` at the top and import `createClient` from `@/lib/supabase/server`, `redirect` from `'next/navigation'`, `revalidatePath` from `'next/cache'`.

**src/components/qr-management/delete-dialog.tsx** — `'use client'`:
- Props: `id: string`, `label: string`, `onDelete: (id: string) => Promise<{ error?: string }>`.
- Use shadcn AlertDialog components.
- AlertDialogTrigger: render a button with Trash2 icon from lucide-react, `text-muted-foreground hover:text-danger` styling, `size="icon"` variant.
- AlertDialogContent: dark theme (bg-raised border-overlay).
  - Title: `Delete "${label}"?`
  - Description: "This QR code will be deactivated. Any printed codes will stop working."
- AlertDialogFooter:
  - Cancel button (AlertDialogCancel).
  - Delete button (AlertDialogAction) with `bg-danger hover:bg-danger/90 text-white`. onClick calls `onDelete(id)`.
- Use `useState` for a local `deleting` state to disable the button during the action.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>deleteQrCode Server Action performs soft delete with RLS protection. DeleteDialog shows named confirmation with cancel/delete options.</done>
</task>

<task type="auto">
  <name>Task 2: Create dashboard list page with QR rows, thumbnails, and actions</name>
  <files>
    src/app/dashboard/page.tsx,
    src/components/dashboard/qr-list.tsx,
    src/components/dashboard/qr-list-row.tsx,
    src/components/dashboard/delete-button.tsx
  </files>
  <action>
**src/app/dashboard/page.tsx** — Server Component (replace the existing stub):
- Import `createClient` from `@/lib/supabase/server`.
- Import `generateQrDataUrl` from `@/lib/qr-generator`.
- Import `QrList` from `@/components/dashboard/qr-list`.
- Import `EmptyState` from `@/components/shared/empty-state`.
- Import `PageHeader` from `@/components/shared/page-header`.
- Export metadata: `{ title: 'Dashboard — FluxQR' }`.
- Fetch user via `supabase.auth.getUser()`. Query `qr_codes` with `.select('*').eq('user_id', user!.id).eq('is_active', true).order('created_at', { ascending: false })`.
- Generate QR thumbnails: `Promise.all((qrCodes ?? []).map(async (qr) => ({ ...qr, dataUrl: await generateQrDataUrl(qr.slug) })))`.
- Render `PageHeader` with title "My QR Codes" and action `{ label: 'New QR', href: '/dashboard/new' }`.
- Conditional: if `qrsWithImages.length === 0`, render `<EmptyState />`. Otherwise render `<QrList qrCodes={qrsWithImages} />`.

**Type for QR with image:** Define `QrCodeWithImage = QrCode & { dataUrl: string }` — export from this file or define inline and pass to QrList.

**src/components/dashboard/qr-list.tsx** — `'use client'`:
- Props: `qrCodes: QrCodeWithImage[]` (import the type).
- Render a `<div className="space-y-2">` containing a `QrListRow` for each QR code.
- Pass `deleteQrCode` server action to each row (import from `@/app/dashboard/[id]/edit/actions`).

**src/components/dashboard/qr-list-row.tsx** — `'use client'`:
- Props: `qr: QrCodeWithImage`, `onDelete: (id: string) => Promise<{ error?: string }>`.
- Layout: flex row on desktop, stack on mobile. Card-like: `bg-raised rounded-lg p-4 border border-overlay`.
- Left: `<img>` tag with `src={qr.dataUrl}`, `alt={qr.label}`, `width={40}`, `height={40}`, `className="rounded"`.
- Center (flex-1):
  - Label: `font-medium text-sm`.
  - Slug: `text-xs text-muted-foreground font-mono` — show as `/q/{slug}`.
  - `PlatformBadge` component with `qr.platform`.
- Right: scan count using `formatScanCount(qr.scan_count)` with `text-muted-foreground text-sm`.
- Actions row: three icon buttons (all `size="icon"` variant, ghost styling):
  1. Edit: Link to `/dashboard/${qr.id}/edit` with Pencil icon.
  2. Download: button calling `downloadQrPng(qr.dataUrl, qr.slug)` with Download icon. Import `downloadQrPng` from `@/lib/qr-generator`.
  3. Delete: `<DeleteButton id={qr.id} label={qr.label} onDelete={onDelete} />`.

**src/components/dashboard/delete-button.tsx** — `'use client'`:
- Props: `id: string`, `label: string`, `onDelete: (id: string) => Promise<{ error?: string }>`.
- Renders `<DeleteDialog id={id} label={label} onDelete={onDelete} />` (import from `@/components/qr-management/delete-dialog`).
- This component exists as a thin wrapper to keep the list row clean.

**Responsive behavior:**
- Desktop: single row with all info visible.
- Mobile (below `md`): stack thumbnail+info, actions below. Use `flex flex-col md:flex-row md:items-center gap-3`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
Dashboard page fetches user's QR codes, generates thumbnails server-side, renders list or empty state. Each row shows thumbnail, label, slug, platform badge, scan count, edit link, download button, and delete button. Delete triggers AlertDialog confirmation and soft-deletes via Server Action. formatScanCount handles compact notation. TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `ls src/app/dashboard/page.tsx src/app/dashboard/\\[id\\]/edit/actions.ts` — exist
- `ls src/components/dashboard/qr-list.tsx src/components/dashboard/qr-list-row.tsx src/components/dashboard/delete-button.tsx` — exist
- `ls src/components/qr-management/delete-dialog.tsx` — exists
</verification>

<success_criteria>
- /dashboard fetches authenticated user's active QR codes ordered by created_at desc
- Each row shows 40px QR thumbnail (server-generated data URL), label, /q/{slug}, platform badge, scan count
- Scan counts >= 1000 display as compact notation (1.2k)
- Empty state shows EmptyState component with CTA to /dashboard/new
- Page header has "New QR" button linking to /dashboard/new
- Edit button links to /dashboard/{id}/edit
- Download button triggers PNG download as {slug}-fluxqr.png
- Delete button opens AlertDialog naming the QR code
- Confirming delete sets is_active=false via Server Action, revalidates /dashboard
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-qr-management/03-03-SUMMARY.md`
</output>
