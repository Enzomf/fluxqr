---
phase: 07-codereview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/use-qr-image.ts
  - src/app/q/[slug]/scanner-landing.tsx
  - src/components/dashboard/delete-button.tsx
  - src/components/dashboard/qr-list-row.tsx
  - src/components/shared/page-header.tsx
  - src/app/actions/check-otp.ts
  - src/app/api/slug-check/route.ts
  - src/components/qr-management/qr-form.tsx
  - src/app/q/[slug]/page.tsx
  - src/app/dashboard/layout.tsx
  - src/app/admin/layout.tsx
  - package.json
autonomous: true
requirements:
  - CR-01
  - CR-02
  - CR-03

must_haves:
  truths:
    - "Dead code (use-qr-image.ts, scanner-landing.tsx, next-themes) is removed from the codebase"
    - "Redundant DeleteButton wrapper is eliminated — QrListRow imports DeleteDialog directly"
    - "Slug availability check queries ALL slugs (not just active) to match DB UNIQUE constraint"
    - "Toast messages in qr-form.tsx are English, not Portuguese"
    - "Scanner page uses createAdminClient() helper instead of inline createServerClient"
    - "check-otp.ts uses static imports instead of dynamic imports"
    - "Dashboard and admin layouts have defense-in-depth comments"
    - "Hardcoded hex values in qr-list-row.tsx are replaced with design tokens"
  artifacts:
    - path: "src/components/dashboard/qr-list-row.tsx"
      provides: "Direct DeleteDialog import (no DeleteButton wrapper) + design tokens"
      contains: "DeleteDialog"
    - path: "src/app/api/slug-check/route.ts"
      provides: "Slug check against all slugs"
    - path: "src/app/q/[slug]/page.tsx"
      provides: "Scanner page using createAdminClient helper"
      contains: "createAdminClient"
  key_links:
    - from: "src/components/dashboard/qr-list-row.tsx"
      to: "src/components/qr-management/delete-dialog.tsx"
      via: "direct import"
      pattern: "import.*DeleteDialog.*delete-dialog"
    - from: "src/app/q/[slug]/page.tsx"
      to: "src/lib/supabase/admin.ts"
      via: "import"
      pattern: "import.*createAdminClient.*admin"
---

<objective>
Fix all code smells, dead code, duplicate patterns, and logic bugs identified in the code review research.

Purpose: Eliminate dead code, redundant wrappers, duplicate patterns, a slug-check logic bug, Portuguese toast strings, and dynamic import anti-patterns — producing a cleaner, more maintainable codebase. Also replaces hardcoded hex values in qr-list-row.tsx with design tokens (consolidated here to avoid parallel write conflict with Plan 07-02).
Output: ~12 files modified, 3 files deleted, clean lint + typecheck + build.
</objective>

<execution_context>
@/Users/enzo.figueiredo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/enzo.figueiredo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-a-complete-codereview-phase-to-ensure-nextjs-best-practices-2026-and-identify-codesmeslls-duplication/07-RESEARCH.md

<interfaces>
<!-- Key files being modified. Executor needs these for context. -->

From src/components/dashboard/qr-list-row.tsx (imports DeleteButton currently):
```tsx
import { DeleteButton } from './delete-button'
// Used as: <DeleteButton id={qr.id} label={qr.label} onDelete={handleDelete} />
```

From src/components/qr-management/delete-dialog.tsx (target direct import):
```tsx
export function DeleteDialog({ id, label, onDelete }: { id: string; label: string; onDelete: (id: string) => void })
```

From src/lib/supabase/admin.ts:
```tsx
export function createAdminClient()
```

From src/app/q/[slug]/page.tsx (inline admin client to replace):
```tsx
import { createServerClient } from '@supabase/ssr'
// Lines 28-39: inline createServerClient with service role key
```

Design token mapping for qr-list-row.tsx hex replacements (from 07-02 interfaces):
| Hardcoded Hex | Tailwind Token Class |
|---------------|---------------------|
| `bg-[#0F172A]` | `bg-surface` |
| `bg-[#1E293B]` | `bg-surface-raised` |
| `bg-[#334155]` | `bg-surface-overlay` |
| `border-[#334155]` | `border-surface-overlay` |
| `bg-[#6366F1]` | `bg-brand-500` |
| `hover:bg-[#4F46E5]` | `hover:bg-brand-600` |
| `text-[#6366F1]` | `text-brand-500` |
| `hover:text-[#4F46E5]` | `hover:text-brand-600` |
| `text-[#818CF8]` | `text-brand-400` |
| `text-[#F43F5E]` | `text-danger` |
| `bg-[#F43F5E]` | `bg-danger` |
| `text-[#F59E0B]` | `text-warning` |
| `text-[#94A3B8]` | `text-slate-400` |
| `text-[#64748B]` | `text-slate-500` |
| `focus:ring-[#6366F1]` | `focus:ring-brand-500` |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead code and redundant wrappers</name>
  <files>
    src/hooks/use-qr-image.ts,
    src/app/q/[slug]/scanner-landing.tsx,
    src/components/dashboard/delete-button.tsx,
    src/components/dashboard/qr-list-row.tsx,
    src/components/shared/page-header.tsx,
    src/app/actions/check-otp.ts,
    package.json
  </files>
  <action>
    1. **Delete `src/hooks/use-qr-image.ts`** — unused hook (confirmed by research, no imports anywhere in codebase). Verify with grep that nothing imports it before deleting.

    2. **Delete `src/app/q/[slug]/scanner-landing.tsx`** — the scanner page always calls `redirect()` before rendering, so ScannerLanding is never used. The research confirms this is dead code. Verify no imports exist before deleting.

    3. **Delete `src/components/dashboard/delete-button.tsx`** — this is a 1:1 passthrough wrapper around DeleteDialog that adds zero value. In `src/components/dashboard/qr-list-row.tsx`, replace the `import { DeleteButton } from './delete-button'` with `import { DeleteDialog } from '@/components/qr-management/delete-dialog'`. Replace all `<DeleteButton ...>` usages with `<DeleteDialog ...>` (same props: id, label, onDelete).

    4. **Remove `next-themes` from `package.json`** — installed but never imported anywhere. The app uses permanent dark mode (`className="dark"` on html). Run `pnpm remove next-themes` to cleanly uninstall.

    5. **Fix `src/components/shared/page-header.tsx`** — remove redundant `cn()` wrapper on static-only class strings. Change `className={cn('flex items-center justify-between')}` to `className="flex items-center justify-between"`. Only remove `cn()` from calls with a single static string argument; keep `cn()` where conditional classes are used.

    6. **Fix `src/app/actions/check-otp.ts`** — convert dynamic imports to static imports at the file top:
       - `const { createClient } = await import('@/lib/supabase/server')` becomes `import { createClient } from '@/lib/supabase/server'`
       - `const { createAdminClient } = await import('@/lib/supabase/admin')` becomes `import { createAdminClient } from '@/lib/supabase/admin'`
       - Test that no circular dependency occurs by running `npx tsc --noEmit` after the change. If it fails, revert to dynamic imports and add a comment: `// Dynamic import required — circular dependency with server.ts`

    7. **Replace hardcoded hex values in `src/components/dashboard/qr-list-row.tsx`** — while the file is already open for the DeleteDialog swap (step 3), also replace ALL hardcoded hex color class values with design tokens using the mapping table in the interfaces section above. This is consolidated here from Plan 07-02 to avoid a parallel write conflict (both plans are Wave 1). Apply the same replacement rules as Plan 07-02: `bg-[#1E293B]` -> `bg-surface-raised`, `text-[#6366F1]` -> `text-brand-500`, etc.
  </action>
  <verify>
    <automated>pnpm lint && npx tsc --noEmit</automated>
  </verify>
  <done>
    - use-qr-image.ts, scanner-landing.tsx, delete-button.tsx are deleted
    - next-themes removed from package.json and pnpm-lock.yaml
    - qr-list-row.tsx imports DeleteDialog directly
    - qr-list-row.tsx uses design tokens (no hardcoded hex values remain)
    - page-header.tsx uses plain className for static strings
    - check-otp.ts uses static imports (or has documented reason for dynamic)
    - Zero lint errors, zero type errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix bugs, duplication, and add documentation comments</name>
  <files>
    src/app/api/slug-check/route.ts,
    src/components/qr-management/qr-form.tsx,
    src/app/q/[slug]/page.tsx,
    src/app/dashboard/layout.tsx,
    src/app/admin/layout.tsx
  </files>
  <action>
    1. **Fix `src/app/api/slug-check/route.ts`** — Remove the `.eq('is_active', true)` filter from the slug availability query. The query should check ALL slugs regardless of active state to match the DB UNIQUE constraint semantics. The result line `return Response.json({ available: data === null })` stays the same.

    2. **Fix `src/components/qr-management/qr-form.tsx`** — Replace Portuguese toast messages with English:
       - `'QR criado com sucesso'` becomes `'QR code created'`
       - `'QR atualizado'` becomes `'QR code updated'`

    3. **Fix `src/app/q/[slug]/page.tsx`** — Replace the inline `createServerClient` admin client construction (the one using `SUPABASE_SERVICE_ROLE_KEY` with empty cookie handlers) with `import { createAdminClient } from '@/lib/supabase/admin'` and `const adminClient = createAdminClient()`. Keep the inline anon client inside the `after()` callback as-is (architecturally justified — no cookies in after callbacks, documented in STATE.md). Also remove the now-unnecessary `import { createServerClient } from '@supabase/ssr'` if it was only used for the admin client (check if the after() callback also uses it — if so, keep the import).

    4. **Add defense-in-depth comments** to `src/app/dashboard/layout.tsx` and `src/app/admin/layout.tsx` — add a brief comment near the auth check: `// Defense-in-depth: middleware is first layer, layout is second`
  </action>
  <verify>
    <automated>pnpm lint && npx tsc --noEmit && npx tsx src/lib/__tests__/redirect.test.ts</automated>
  </verify>
  <done>
    - slug-check route queries all slugs (no is_active filter)
    - Toast messages are in English
    - Scanner page uses createAdminClient() for the admin check
    - Dashboard and admin layouts have defense-in-depth comments
    - All tests pass, lint clean, type clean
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `pnpm lint` — zero errors, zero warnings
2. `npx tsc --noEmit` — zero type errors
3. `npx tsx src/lib/__tests__/redirect.test.ts` — all redirect tests pass
4. Verify deleted files don't exist: `ls src/hooks/use-qr-image.ts src/app/q/[slug]/scanner-landing.tsx src/components/dashboard/delete-button.tsx` should all fail
5. `grep -r "next-themes" package.json` — no match
6. `grep -n 'bg-\[#\|text-\[#\|border-\[#' src/components/dashboard/qr-list-row.tsx` — zero matches (hex values replaced with tokens)
</verification>

<success_criteria>
- 3 dead code files deleted (use-qr-image.ts, scanner-landing.tsx, delete-button.tsx)
- 1 dead dependency removed (next-themes)
- Slug check bug fixed (checks all slugs)
- Portuguese toast strings replaced with English
- Scanner page admin client uses helper function
- Static imports in check-otp.ts (or documented reason)
- Redundant cn() removed from page-header.tsx
- Defense-in-depth comments added
- qr-list-row.tsx hex values replaced with design tokens
- Zero lint errors, zero type errors, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-a-complete-codereview-phase-to-ensure-nextjs-best-practices-2026-and-identify-codesmeslls-duplication/07-01-SUMMARY.md`
</output>
