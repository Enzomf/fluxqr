---
phase: 07-codereview
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - src/app/layout.tsx
  - src/app/robots.ts
  - src/app/sitemap.ts
  - src/app/dashboard/error.tsx
  - src/app/admin/error.tsx
  - src/app/admin/page.tsx
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/CONCERNS.md
  - .planning/codebase/CONVENTIONS.md
autonomous: true
requirements:
  - CR-05
  - CR-06
  - CR-07

must_haves:
  truths:
    - "Root layout has metadataBase, Open Graph tags, and Twitter card metadata"
    - "robots.ts disallows /dashboard/ and /admin/ routes"
    - "sitemap.ts lists public routes (/, /login)"
    - "Dashboard route has error.tsx boundary with a Try Again button"
    - "Admin route has error.tsx boundary with a Try Again button"
    - "Codebase maps reflect the actual current implementation state"
    - "A brief summary report exists documenting all code review changes"
  artifacts:
    - path: "src/app/robots.ts"
      provides: "robots.txt generation"
      exports: ["default"]
    - path: "src/app/sitemap.ts"
      provides: "sitemap.xml generation"
      exports: ["default"]
    - path: "src/app/dashboard/error.tsx"
      provides: "Dashboard error boundary"
      contains: "'use client'"
    - path: "src/app/admin/error.tsx"
      provides: "Admin error boundary"
      contains: "'use client'"
    - path: ".planning/codebase/ARCHITECTURE.md"
      provides: "Updated architecture documentation"
    - path: ".planning/codebase/CONCERNS.md"
      provides: "Updated concerns documentation"
    - path: ".planning/codebase/CONVENTIONS.md"
      provides: "Updated conventions documentation"
  key_links:
    - from: "src/app/layout.tsx"
      to: "Next.js metadata API"
      via: "export const metadata: Metadata"
      pattern: "metadataBase.*new URL"
    - from: "src/app/robots.ts"
      to: "Next.js MetadataRoute"
      via: "typed robots export"
      pattern: "MetadataRoute\\.Robots"
---

<objective>
Add SEO metadata, error boundaries, and update codebase maps to complete the code review phase.

Purpose: Close the remaining gaps identified in the research: missing SEO tags (metadataBase, OG, robots.txt, sitemap), missing error boundaries on routes that query the database, and outdated codebase documentation. Produce a summary report of all code review changes.
Output: 5 new files created, 4 files updated, codebase maps rewritten, summary report produced.
</objective>

<execution_context>
@/Users/enzo.figueiredo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/enzo.figueiredo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-a-complete-codereview-phase-to-ensure-nextjs-best-practices-2026-and-identify-codesmeslls-duplication/07-RESEARCH.md
@.planning/phases/07-a-complete-codereview-phase-to-ensure-nextjs-best-practices-2026-and-identify-codesmeslls-duplication/07-01-SUMMARY.md
@.planning/phases/07-a-complete-codereview-phase-to-ensure-nextjs-best-practices-2026-and-identify-codesmeslls-duplication/07-02-SUMMARY.md

<interfaces>
<!-- From src/app/layout.tsx — current metadata export to enhance -->
<!-- Read the file to see exact current metadata structure before modifying -->

From 07-RESEARCH.md — recommended metadata pattern:
```tsx
import type { Metadata } from 'next'

export const metadata: Metadata = {
  metadataBase: new URL(process.env.NEXT_PUBLIC_SITE_URL ?? 'https://fluxqr.app'),
  title: {
    default: 'FluxQR — Smart QR Links for Messaging',
    template: '%s — FluxQR',
  },
  description: 'Create QR codes that open WhatsApp and SMS with pre-filled messages.',
  openGraph: {
    siteName: 'FluxQR',
    type: 'website',
  },
}
```

From 07-RESEARCH.md — recommended robots.ts:
```ts
import type { MetadataRoute } from 'next'

export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: '*',
      allow: '/',
      disallow: ['/dashboard/', '/admin/'],
    },
  }
}
```

From 07-RESEARCH.md — recommended error.tsx pattern:
```tsx
'use client'

export default function DashboardError({ reset }: { reset: () => void }) {
  return (
    <div className="flex flex-col items-center justify-center min-h-[50vh] gap-4 text-center">
      <h2 className="text-lg font-semibold">Something went wrong</h2>
      <button
        type="button"
        onClick={reset}
        className="bg-brand-500 hover:bg-brand-600 text-white rounded-md px-4 py-2 text-sm"
      >
        Try again
      </button>
    </div>
  )
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SEO metadata and error boundaries</name>
  <files>
    src/app/layout.tsx,
    src/app/robots.ts,
    src/app/sitemap.ts,
    src/app/dashboard/error.tsx,
    src/app/admin/error.tsx,
    src/app/admin/page.tsx
  </files>
  <action>
    1. **Enhance `src/app/layout.tsx` metadata** — Read the current file first. Update the existing `metadata` export to include:
       - `metadataBase: new URL(process.env.NEXT_PUBLIC_SITE_URL ?? 'https://fluxqr.app')`
       - Title template: `title: { default: 'FluxQR — Smart QR Links for Messaging', template: '%s — FluxQR' }`
       - `description: 'Create QR codes that open WhatsApp and SMS with pre-filled messages.'`
       - `openGraph: { siteName: 'FluxQR', type: 'website' }`
       - `twitter: { card: 'summary' }`
       - Import `Metadata` type from `next` if not already imported
       - Do NOT change anything else in the layout (fonts, body, Toaster, etc.)

    2. **Create `src/app/robots.ts`** — New file. Export a typed robots function that:
       - Allows `/` for all user agents
       - Disallows `/dashboard/` and `/admin/`
       - Uses `MetadataRoute.Robots` type from `next`

    3. **Create `src/app/sitemap.ts`** — New file. Export a typed sitemap function that:
       - Lists public routes: `/` and `/login`
       - Uses `MetadataRoute.Sitemap` type from `next`
       - Uses `process.env.NEXT_PUBLIC_SITE_URL ?? 'https://fluxqr.app'` as the base URL

    4. **Create `src/app/dashboard/error.tsx`** — New file. A 'use client' error boundary component that:
       - Receives `{ error, reset }` props (standard Next.js error boundary signature)
       - Shows "Something went wrong" heading
       - Shows a "Try again" button that calls `reset()`
       - Uses design tokens (bg-brand-500 hover:bg-brand-600) not hardcoded hex
       - Uses a `useEffect` to `console.error(error)` for debugging

    5. **Create `src/app/admin/error.tsx`** — New file. Same pattern as dashboard error boundary. Can be identical in structure.

    6. **Fix `src/app/admin/page.tsx` metadata** — Add `Metadata` type to the metadata export: `export const metadata: Metadata = { title: 'Admin' }`. Import `Metadata` from `next`. The title template in root layout will produce "Admin — FluxQR".
  </action>
  <verify>
    <automated>pnpm lint && npx tsc --noEmit && pnpm build</automated>
  </verify>
  <done>
    - Root layout has metadataBase, OG, Twitter card, title template
    - robots.ts exists and disallows /dashboard/ and /admin/
    - sitemap.ts exists with / and /login routes
    - Dashboard and admin both have error.tsx boundaries
    - Admin page metadata is typed
    - Build succeeds with all new files
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite codebase maps and produce summary report</name>
  <files>
    .planning/codebase/ARCHITECTURE.md,
    .planning/codebase/CONCERNS.md,
    .planning/codebase/CONVENTIONS.md
  </files>
  <action>
    Read the entire `src/` directory structure and all key files to produce accurate, current documentation.

    1. **Rewrite `.planning/codebase/ARCHITECTURE.md`** — Full rewrite reflecting the actual current architecture:
       - Route structure (all routes: /, /login, /dashboard, /dashboard/*, /admin, /admin/*, /q/[slug], /api/slug-check, /auth/callback)
       - Server/Client component boundaries (which are SC, which are CC)
       - Data flow: Server Components fetch -> props to Client Components -> Server Actions for mutations -> router.refresh()
       - Supabase client variants: server.ts (SSR cookies), client.ts (browser), admin.ts (service role), middleware.ts
       - Auth flow: Google OAuth -> callback -> session cookie -> middleware guard -> layout guard (defense-in-depth)
       - Public flow: phone verify -> OTP -> freemium gate -> public QR creation
       - Admin flow: middleware role check -> layout role check -> admin actions
       - Modal dialog flow: QrFormDialog state machine in QrList

    2. **Rewrite `.planning/codebase/CONCERNS.md`** — Full rewrite with actual post-implementation concerns:
       - List any remaining concerns (not pre-implementation hypotheticals)
       - Scanner bundle budget (untested with analyzer)
       - Telegram removed but DB platform CHECK still includes it
       - phone_usage table has no RLS (by design — admin-only access)
       - types/supabase.ts never generated (manual types in use)

    3. **Update `.planning/codebase/CONVENTIONS.md`** — Update to reflect patterns that evolved over 7 phases:
       - Verify existing conventions are still accurate
       - Add new conventions discovered: useActionState pattern, router.refresh() for revalidation, cn() for class merging, design token usage, defense-in-depth auth pattern, Server Actions returning FormState with success/id fields
       - Remove any conventions that are no longer accurate

    4. **Create summary report** at `.planning/phases/07-a-complete-codereview-phase-to-ensure-nextjs-best-practices-2026-and-identify-codesmeslls-duplication/07-REVIEW-REPORT.md` — Brief summary documenting:
       - Total files affected by phase
       - Changes grouped by category (dead code, tokens, SEO, error boundaries, bug fixes, documentation)
       - Key improvements
       - Remaining items for future consideration
  </action>
  <verify>
    <automated>test -f .planning/codebase/ARCHITECTURE.md && test -f .planning/codebase/CONCERNS.md && test -f .planning/codebase/CONVENTIONS.md && test -f ".planning/phases/07-a-complete-codereview-phase-to-ensure-nextjs-best-practices-2026-and-identify-codesmeslls-duplication/07-REVIEW-REPORT.md" && echo "All docs exist"</automated>
  </verify>
  <done>
    - ARCHITECTURE.md reflects actual route structure, data flow, auth flow, modal flow
    - CONCERNS.md lists real post-implementation concerns, not hypotheticals
    - CONVENTIONS.md updated with patterns from all 7 phases
    - 07-REVIEW-REPORT.md summarizes the entire code review phase
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `pnpm lint && npx tsc --noEmit` — clean
2. `pnpm build` — succeeds
3. All 4 documentation files exist and have substantive content
4. `npx tsx src/lib/__tests__/redirect.test.ts` — redirect tests still pass
</verification>

<success_criteria>
- SEO metadata enhanced in root layout (metadataBase, OG, Twitter, title template)
- robots.ts and sitemap.ts created
- Error boundaries for dashboard and admin routes
- Codebase maps fully rewritten to reflect current state
- Summary report produced documenting all phase 07 changes
- Full build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/07-a-complete-codereview-phase-to-ensure-nextjs-best-practices-2026-and-identify-codesmeslls-duplication/07-03-SUMMARY.md`
</output>
