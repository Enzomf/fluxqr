---
phase: 07-codereview
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/home-client.tsx
  - src/app/admin/layout.tsx
  - src/components/qr-management/qr-form.tsx
  - src/components/qr-management/qr-form-dialog.tsx
  - src/components/qr-management/delete-dialog.tsx
  - src/components/public/public-qr-form.tsx
  - src/components/public/freemium-gate.tsx
  - src/components/admin/user-table.tsx
  - src/components/dashboard/phone-verify-dialog.tsx
autonomous: true
requirements:
  - CR-04

must_haves:
  truths:
    - "No hardcoded hex color values (#0F172A, #1E293B, #334155, #6366F1, #4F46E5, #818CF8, #F43F5E, #F59E0B, #94A3B8, #64748B) appear in any source file outside globals.css"
    - "All replaced classes use design tokens (bg-surface, bg-surface-raised, bg-surface-overlay, text-brand-500, bg-brand-500, etc.)"
    - "Visual appearance is unchanged (token values match the hex values being replaced)"
  artifacts:
    - path: "src/app/home-client.tsx"
      provides: "Home page with design tokens"
    - path: "src/components/qr-management/qr-form.tsx"
      provides: "QR form with design tokens"
    - path: "src/components/admin/user-table.tsx"
      provides: "Admin table with design tokens"
  key_links:
    - from: "all modified files"
      to: "src/app/globals.css"
      via: "Tailwind token classes"
      pattern: "bg-surface|bg-surface-raised|bg-surface-overlay|text-brand-500|bg-brand-500|bg-brand-600|text-danger|bg-danger|text-warning"
---

<objective>
Replace all hardcoded hex color values with Tailwind design tokens across the entire codebase.

Purpose: Eliminate color drift risk. When brand or surface colors change, only globals.css needs updating instead of hunting through 10+ component files. This is the single most widespread code smell identified in the research.
Output: ~9 files updated with zero visual changes — tokens map to identical hex values. (qr-list-row.tsx hex replacements are handled in Plan 07-01 to avoid parallel write conflict.)
</objective>

<execution_context>
@/Users/enzo.figueiredo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/enzo.figueiredo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-a-complete-codereview-phase-to-ensure-nextjs-best-practices-2026-and-identify-codesmeslls-duplication/07-RESEARCH.md

<interfaces>
<!-- Token mapping from globals.css @theme inline — this is the source of truth -->

| Hardcoded Hex | Tailwind Token Class | Semantic Name |
|---------------|---------------------|---------------|
| `bg-[#0F172A]` | `bg-surface` | canvas/background |
| `bg-[#1E293B]` | `bg-surface-raised` | card/raised surface |
| `bg-[#334155]` | `bg-surface-overlay` | overlay |
| `border-[#334155]` | `border-surface-overlay` | border on overlay |
| `bg-[#6366F1]` | `bg-brand-500` | brand primary |
| `hover:bg-[#4F46E5]` | `hover:bg-brand-600` | brand hover |
| `text-[#6366F1]` | `text-brand-500` | brand text |
| `hover:text-[#4F46E5]` | `hover:text-brand-600` | brand text hover |
| `text-[#818CF8]` | `text-brand-400` | brand light text |
| `bg-[#6366F1]/10` | `bg-brand-500/10` | brand background muted |
| `text-[#F43F5E]` | `text-danger` | danger/error text |
| `bg-[#F43F5E]` | `bg-danger` | danger/error bg |
| `hover:bg-[#F43F5E]/90` | `hover:bg-danger/90` | danger hover |
| `bg-[#F43F5E]/10` | `bg-danger/10` | danger muted bg |
| `border-[#F43F5E]/40` | `border-danger/40` | danger border |
| `text-[#F59E0B]` | `text-warning` | warning text |
| `text-[#94A3B8]` | `text-slate-400` | muted text |
| `text-[#64748B]` | `text-slate-500` | muted text darker |
| `focus:ring-[#6366F1]` | `focus:ring-brand-500` | focus ring brand |
| `bg-[#334155]/30` | `bg-surface-overlay/30` | overlay faded |

Note: `border-[#334155]` should become `border-surface-overlay`. Verify this token generates the correct utility class in Tailwind v4. If `border-surface-overlay` doesn't work (Tailwind v4 may need `border-[var(--color-surface-overlay)]`), use the closest working pattern.

Note: `src/components/dashboard/qr-list-row.tsx` hex replacements are handled in Plan 07-01 (which also modifies that file for the DeleteDialog swap). Do NOT modify qr-list-row.tsx in this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded hex values with design tokens across all components</name>
  <files>
    src/app/home-client.tsx,
    src/app/admin/layout.tsx,
    src/components/qr-management/qr-form.tsx,
    src/components/qr-management/qr-form-dialog.tsx,
    src/components/qr-management/delete-dialog.tsx,
    src/components/public/public-qr-form.tsx,
    src/components/public/freemium-gate.tsx,
    src/components/admin/user-table.tsx,
    src/components/dashboard/phone-verify-dialog.tsx
  </files>
  <action>
    For each file listed above:
    1. Read the file
    2. Replace ALL hardcoded hex class values using the token mapping table in the interfaces section
    3. Write the updated file

    **Replacement rules:**
    - `bg-[#0F172A]` -> `bg-surface`
    - `bg-[#1E293B]` -> `bg-surface-raised`
    - `bg-[#334155]` and `bg-[#334155]/XX` -> `bg-surface-overlay` and `bg-surface-overlay/XX`
    - `border-[#334155]` -> `border-surface-overlay`
    - `bg-[#6366F1]` -> `bg-brand-500`
    - `hover:bg-[#4F46E5]` -> `hover:bg-brand-600`
    - `text-[#6366F1]` -> `text-brand-500`
    - `hover:text-[#4F46E5]` -> `hover:text-brand-600`
    - `text-[#818CF8]` -> `text-brand-400`
    - `bg-[#6366F1]/10` -> `bg-brand-500/10`
    - `text-[#F43F5E]` -> `text-danger`
    - `bg-[#F43F5E]` -> `bg-danger`
    - `hover:bg-[#F43F5E]/90` -> `hover:bg-danger/90`
    - `bg-[#F43F5E]/10` -> `bg-danger/10`
    - `border-[#F43F5E]/40` -> `border-danger/40`
    - `text-[#F59E0B]` -> `text-warning`
    - `text-[#94A3B8]` -> `text-slate-400`
    - `text-[#64748B]` -> `text-slate-500`
    - `focus:ring-[#6366F1]` -> `focus:ring-brand-500`

    **IMPORTANT:** Also scan for any hardcoded hex values NOT in the mapping table. If found, determine the correct token from globals.css and replace. If no matching token exists, leave the hardcoded value and note it.

    **IMPORTANT:** `src/components/dashboard/phone-verify-dialog.tsx` was flagged as "suspected" in research — read it and check for hardcoded hex values. Only modify if they exist.

    **IMPORTANT:** Do NOT modify any files in `src/components/ui/` (shadcn primitives — CLAUDE.md rule: do not hand-edit).

    **IMPORTANT:** Do NOT modify `src/components/dashboard/qr-list-row.tsx` — its hex replacements are handled in Plan 07-01 to avoid a parallel write conflict.

    After all replacements, run `grep -rn '#[0-9A-Fa-f]\{6\}' src/ --include='*.tsx' --include='*.ts' | grep -v 'globals.css' | grep -v 'node_modules' | grep -v 'components/ui/'` to verify no hardcoded hex values remain outside globals.css and shadcn.
  </action>
  <verify>
    <automated>pnpm lint && npx tsc --noEmit</automated>
  </verify>
  <done>
    - Zero hardcoded hex color values in any src/ file outside globals.css and components/ui/
    - All files use semantic design tokens (bg-surface, text-brand-500, etc.)
    - qr-list-row.tsx was NOT touched by this plan (handled by 07-01)
    - Lint clean, type clean
    - No visual changes (tokens map to identical hex values)
  </done>
</task>

</tasks>

<verification>
After task completes:
1. `pnpm lint` — zero errors
2. `npx tsc --noEmit` — zero type errors
3. `grep -rn 'bg-\[#\|text-\[#\|border-\[#\|ring-\[#\|hover:bg-\[#\|hover:text-\[#\|focus:ring-\[#' src/ --include='*.tsx' --include='*.ts' | grep -v globals.css | grep -v 'components/ui/'` — zero matches (no hardcoded hex in class names)
4. `pnpm build` — build succeeds
</verification>

<success_criteria>
- All ~9 files updated with design tokens replacing hardcoded hex values
- Zero hardcoded hex color class values in src/ (excluding globals.css and components/ui/)
- qr-list-row.tsx not modified (no parallel write conflict)
- Lint, typecheck, and build all pass
- Visual appearance unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/07-a-complete-codereview-phase-to-ensure-nextjs-best-practices-2026-and-identify-codesmeslls-duplication/07-02-SUMMARY.md`
</output>
