---
phase: 08-add-unit-tests-to-all-components-and-services
plan: 04
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/qr-management/delete-dialog.test.tsx
  - src/components/qr-management/platform-selector.test.tsx
  - src/components/qr-management/slug-input.test.tsx
  - src/components/qr-management/qr-type-select.test.tsx
  - src/components/qr-management/qr-form.test.tsx
  - src/components/qr-management/qr-form-dialog.test.tsx
autonomous: true
requirements:
  - TEST-QR-MGMT

must_haves:
  truths:
    - "DeleteDialog calls onDelete with the correct id when user confirms (DEL-01, DEL-02)"
    - "PlatformSelector is disabled in edit mode with tooltip text (EDIT-02)"
    - "SlugInput normalizes input to lowercase with hyphens and shows status indicators"
    - "QrTypeSelect calls onSelect with 'default' or 'custom' when user clicks a card"
    - "QrForm renders all form fields and shows error messages from state"
    - "QrFormDialog shows QR type grid on create, form on edit"
  artifacts:
    - path: "src/components/qr-management/delete-dialog.test.tsx"
      provides: "Tests for DeleteDialog confirm flow and soft-delete handler"
      min_lines: 25
    - path: "src/components/qr-management/platform-selector.test.tsx"
      provides: "Tests for PlatformSelector disabled state and options"
      min_lines: 20
    - path: "src/components/qr-management/slug-input.test.tsx"
      provides: "Tests for SlugInput normalization and status indicators"
      min_lines: 25
    - path: "src/components/qr-management/qr-type-select.test.tsx"
      provides: "Tests for QrTypeSelect card selection"
      min_lines: 15
    - path: "src/components/qr-management/qr-form.test.tsx"
      provides: "Tests for QrForm field rendering, error display, mode behavior"
      min_lines: 30
    - path: "src/components/qr-management/qr-form-dialog.test.tsx"
      provides: "Tests for QrFormDialog step navigation and create/edit modes"
      min_lines: 30
  key_links:
    - from: "src/components/qr-management/delete-dialog.test.tsx"
      to: "src/components/qr-management/delete-dialog.tsx"
      via: "render, click trigger, click confirm"
      pattern: "onDelete.*toHaveBeenCalledWith"
    - from: "src/components/qr-management/qr-form.test.tsx"
      to: "src/components/qr-management/qr-form.tsx"
      via: "render with mocked action prop"
      pattern: "action.*vi\\.fn"
---

<objective>
Write unit tests for all QR management components: delete-dialog, platform-selector, slug-input, qr-type-select, qr-form, and qr-form-dialog. These are the core CRUD interaction components.

Purpose: Validates business rules for QR code creation, editing, deletion, and the dialog-based flow. Tests assert soft-delete behavior (DEL-02), platform read-only after creation (EDIT-02), and slug validation (CREATE-02).
Output: 6 test files covering all QR management components.
</objective>

<execution_context>
@/Users/enzo.figueiredo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/enzo.figueiredo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-add-unit-tests-to-all-components-and-services/08-CONTEXT.md
@.planning/phases/08-add-unit-tests-to-all-components-and-services/08-RESEARCH.md
@.planning/phases/08-add-unit-tests-to-all-components-and-services/08-01-SUMMARY.md

<interfaces>
<!-- Read source files before writing tests to understand exact props and behavior -->

From src/components/qr-management/delete-dialog.tsx:
```typescript
interface DeleteDialogProps {
  id: string; label: string;
  onDelete: (id: string) => Promise<{ error?: string }>
}
export function DeleteDialog({ id, label, onDelete }: DeleteDialogProps)
// Uses shadcn AlertDialog. Trigger button: aria-label="Delete QR code"
// Confirm button text: 'Delete' (or 'Deleting...' when pending)
// Cancel button text: 'Cancel'
```

From src/components/qr-management/platform-selector.tsx:
```typescript
interface PlatformSelectorProps {
  defaultValue?: Platform; disabled?: boolean;
  error?: string[]; onValueChange?: (value: Platform) => void
}
export function PlatformSelector({ defaultValue, disabled, error, onValueChange }: PlatformSelectorProps)
// Uses shadcn Select. When disabled, wraps trigger in Tooltip with "Platform cannot be changed after creation"
```

From src/components/qr-management/slug-input.tsx:
```typescript
interface SlugInputProps { defaultValue?: string; currentSlug?: string; error?: string[] }
export function SlugInput({ defaultValue, currentSlug, error }: SlugInputProps)
// Uses useSlugCheck hook. Normalizes input: toLowerCase + replace spaces with hyphens
// Shows status indicators: Checking..., Available, Already taken, Invalid format
```

From src/components/qr-management/qr-type-select.tsx:
```typescript
interface QrTypeSelectProps { onSelect: (type: 'default' | 'custom') => void }
export function QrTypeSelect({ onSelect }: QrTypeSelectProps)
// Two card buttons: "Meu QR Code" (default) and "Custom QR" (custom)
```

From src/components/qr-management/qr-form.tsx:
```typescript
interface QrFormProps {
  action: (prevState: FormState, formData: FormData) => Promise<FormState>
  defaultValues?: Partial<QrCode>; mode: 'create' | 'edit'
  verifiedPhone?: string | null; qrType?: 'default' | 'custom'
  onSuccess?: (id?: string) => void
}
export function QrForm({ action, defaultValues, mode, verifiedPhone, qrType, onSuccess }: QrFormProps)
```

From src/components/qr-management/qr-form-dialog.tsx:
```typescript
interface QrFormDialogProps {
  open: boolean; onOpenChange: (open: boolean) => void
  qr?: QrCodeWithImage | null; verifiedPhone: string | null
  onEditSuccess?: (id: string) => void
}
export function QrFormDialog({ open, onOpenChange, qr, verifiedPhone, onEditSuccess }: QrFormDialogProps)
// Create mode: shows QrTypeSelect grid first, then form
// Edit mode: shows form directly with pre-filled values
```

From src/app/dashboard/qr-actions.ts:
```typescript
export type FormState = {
  success?: boolean; errors?: { [field: string]: string[] }
  message?: string | null; id?: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test delete-dialog, platform-selector, slug-input, and qr-type-select</name>
  <files>src/components/qr-management/delete-dialog.test.tsx, src/components/qr-management/platform-selector.test.tsx, src/components/qr-management/slug-input.test.tsx, src/components/qr-management/qr-type-select.test.tsx</files>
  <action>
Read each source file before writing its test to understand exact rendering and behavior.

1. **DeleteDialog tests** — `src/components/qr-management/delete-dialog.test.tsx`:
   - Renders trigger button with aria-label="Delete QR code"
   - Opens dialog content when trigger is clicked (userEvent.click)
   - Dialog shows the QR label in the title (e.g., 'Delete "My QR"?')
   - Dialog shows deactivation warning text ("will be deactivated")
   - Calls onDelete with the correct id when "Delete" button is confirmed (DEL-01)
   - Does NOT call onDelete when "Cancel" is clicked
   - Business rule DEL-02: onDelete is a handler function (soft delete) — the component never does a direct database DELETE

2. **PlatformSelector tests** — `src/components/qr-management/platform-selector.test.tsx`:
   - Renders a select/trigger element
   - When disabled=true, renders tooltip text "Platform cannot be changed after creation" (EDIT-02 business rule)
   - When disabled=true, the select trigger has disabled styling (opacity-50)
   - Renders error message when error prop is provided
   - Note: Testing shadcn Select interactions (opening dropdown, selecting) may be complex in jsdom. Focus on rendering/disabled state, not dropdown interaction.

3. **SlugInput tests** — `src/components/qr-management/slug-input.test.tsx`:
   - Mock useSlugCheck: `vi.mock('@/hooks/use-slug-check', () => ({ useSlugCheck: vi.fn().mockReturnValue('idle') }))`
   - Renders input with name="slug"
   - Renders with defaultValue when provided
   - Shows "Available" text when useSlugCheck returns 'available'
   - Shows "Already taken" text when useSlugCheck returns 'taken'
   - Shows "Invalid format" text when useSlugCheck returns 'invalid'
   - Shows "Checking..." text when useSlugCheck returns 'checking'
   - Renders error text when error prop provided
   - Input normalizes value: typing "My Slug" produces "my-slug" (lowercase + spaces to hyphens)

4. **QrTypeSelect tests** — `src/components/qr-management/qr-type-select.test.tsx`:
   - Renders "Meu QR Code" button text
   - Renders "Custom QR" button text
   - Calls onSelect('default') when "Meu QR Code" card is clicked
   - Calls onSelect('custom') when "Custom QR" card is clicked

Run: `pnpm vitest run src/components/qr-management/delete-dialog src/components/qr-management/platform-selector src/components/qr-management/slug-input src/components/qr-management/qr-type-select`
  </action>
  <verify>
    <automated>cd /Users/enzo.figueiredo/www/fluxqr && pnpm vitest run src/components/qr-management/delete-dialog src/components/qr-management/platform-selector src/components/qr-management/slug-input src/components/qr-management/qr-type-select 2>&1 | tail -15</automated>
  </verify>
  <done>DeleteDialog confirm flow tested (DEL-01, DEL-02). PlatformSelector disabled state + tooltip text verified (EDIT-02). SlugInput status indicators and input normalization tested. QrTypeSelect card selection callbacks verified.</done>
</task>

<task type="auto">
  <name>Task 2: Test qr-form and qr-form-dialog</name>
  <files>src/components/qr-management/qr-form.test.tsx, src/components/qr-management/qr-form-dialog.test.tsx</files>
  <action>
Read `src/components/qr-management/qr-form.tsx` and `src/components/qr-management/qr-form-dialog.tsx` first to understand their exact imports, props, and rendering behavior.

1. **QrForm tests** — `src/components/qr-management/qr-form.test.tsx`:
   - Mock all imports that QrForm uses:
     - `vi.mock('@/hooks/use-slug-check', () => ({ useSlugCheck: vi.fn().mockReturnValue('idle') }))`
     - `vi.mock('@/components/dashboard/phone-verify-dialog', () => ({ PhoneVerifyDialog: () => null }))`
     - Mock sonner's toast: `vi.mock('sonner', () => ({ toast: { success: vi.fn() } }))`
   - Pass a mock action: `vi.fn().mockResolvedValue({ errors: {}, message: null })` as the action prop
   - **Create mode tests:**
     - Renders Label, Slug, Platform, Contact Target, and Default Message fields when qrType='custom'
     - Does not render Default Message field when qrType='default'
     - Shows "Phone verification required" banner when verifiedPhone is null
     - Does not show phone verification banner when verifiedPhone is provided
     - Shows read-only phone display when verifiedPhone is provided (displays the phone number)
   - **Edit mode tests:**
     - Pre-fills form fields from defaultValues
     - Shows read-only contact target (not editable input)
   - **Error display tests:**
     - Renders error message from state.message (top-level error banner)
     - Renders field-level error when state.errors has field entries (test by providing action that returns errors)

2. **QrFormDialog tests** — `src/components/qr-management/qr-form-dialog.test.tsx`:
   - Mock Server Actions: `vi.mock('@/app/dashboard/qr-actions', () => ({ createQrCode: vi.fn(), updateQrCode: { bind: vi.fn().mockReturnValue(vi.fn()) } }))`
   - Mock QrForm and QrTypeSelect to avoid deep rendering complexity:
     ```
     vi.mock('./qr-type-select', () => ({ QrTypeSelect: ({ onSelect }: any) => <button onClick={() => onSelect('custom')}>mock-type-select</button> }))
     vi.mock('./qr-form', () => ({ QrForm: () => <div data-testid="mock-qr-form">form</div> }))
     ```
   - **Create mode (qr=null):**
     - When open=true: renders dialog with title "New QR Code"
     - Shows QR type selection grid first (step='grid')
     - After selecting a type: advances to form step
     - Shows submit button only on form step
   - **Edit mode (qr={...data}):**
     - When open=true: renders dialog with title containing the QR label (e.g., "Edit: My QR")
     - Shows form directly (skips grid step)
     - Shows "Save Changes" submit button
   - **Dialog close:**
     - When open=false: dialog content not in document
   - Note: If mocking child components is simpler, prefer that over testing the full tree. The children have their own dedicated tests.

Run: `pnpm vitest run src/components/qr-management/qr-form`
  </action>
  <verify>
    <automated>cd /Users/enzo.figueiredo/www/fluxqr && pnpm vitest run src/components/qr-management/ 2>&1 | tail -15</automated>
  </verify>
  <done>QrForm tests cover create/edit modes, field rendering, error display, phone verification state. QrFormDialog tests cover step navigation (grid->form for create, direct form for edit), dialog title, submit button labels.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run src/components/qr-management/` passes all tests in all 6 files
- DeleteDialog tests validate soft-delete business rule (DEL-01, DEL-02)
- PlatformSelector tests validate read-only after creation (EDIT-02)
- QrForm tests cover both create and edit modes with error handling
- QrFormDialog tests validate the two-step create flow vs direct edit flow
</verification>

<success_criteria>
- All 6 QR management component test files pass
- Business rules validated: DEL-01 (dialog names QR code), DEL-02 (soft delete via handler), EDIT-02 (platform read-only)
- QrForm tested in create mode (with/without phone, with/without custom message) and edit mode
- QrFormDialog step navigation tested for create (grid -> form) and edit (direct form)
</success_criteria>

<output>
After completion, create `.planning/phases/08-add-unit-tests-to-all-components-and-services/08-04-SUMMARY.md`
</output>
