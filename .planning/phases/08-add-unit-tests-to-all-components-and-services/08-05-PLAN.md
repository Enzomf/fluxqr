---
phase: 08-add-unit-tests-to-all-components-and-services
plan: 05
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/public/freemium-gate.test.tsx
  - src/components/public/otp-verify-form.test.tsx
  - src/components/public/phone-verify-form.test.tsx
  - src/components/public/public-qr-form.test.tsx
  - src/components/public/public-qr-result-dialog.test.tsx
  - src/components/public/qr-type-grid.test.tsx
  - src/components/admin/user-table.test.tsx
  - src/components/admin/user-qr-table.test.tsx
autonomous: true
requirements:
  - TEST-PUBLIC
  - TEST-ADMIN

must_haves:
  truths:
    - "FreemiumGate renders the 5-use limit message and Google sign-in button"
    - "OtpVerifyForm renders 6-digit OTP input and calls verify action"
    - "PhoneVerifyForm renders phone input and calls send verification action"
    - "PublicQrForm renders form fields for public QR creation"
    - "PublicQrResultDialog shows generated QR code and download option when open"
    - "QrTypeGrid renders two option cards and calls onSelect"
    - "UserTable renders rows for each user with email and QR count"
    - "UserQrTable renders QR code details for a specific user"
  artifacts:
    - path: "src/components/public/freemium-gate.test.tsx"
      provides: "Tests for FreemiumGate limit message and CTA"
      min_lines: 15
    - path: "src/components/public/otp-verify-form.test.tsx"
      provides: "Tests for OTP input rendering and verify callback"
      min_lines: 20
    - path: "src/components/public/phone-verify-form.test.tsx"
      provides: "Tests for phone input and send verification"
      min_lines: 20
    - path: "src/components/public/public-qr-form.test.tsx"
      provides: "Tests for public QR form field rendering"
      min_lines: 20
    - path: "src/components/public/public-qr-result-dialog.test.tsx"
      provides: "Tests for result dialog QR display and download"
      min_lines: 20
    - path: "src/components/public/qr-type-grid.test.tsx"
      provides: "Tests for QR type card selection"
      min_lines: 15
    - path: "src/components/admin/user-table.test.tsx"
      provides: "Tests for admin user table rendering"
      min_lines: 20
    - path: "src/components/admin/user-qr-table.test.tsx"
      provides: "Tests for admin user QR detail table"
      min_lines: 20
  key_links:
    - from: "src/components/public/freemium-gate.test.tsx"
      to: "src/components/public/freemium-gate.tsx"
      via: "render and assert limit text"
      pattern: "5 free QR codes"
    - from: "src/components/admin/user-table.test.tsx"
      to: "src/components/admin/user-table.tsx"
      via: "render with user data array"
      pattern: "import.*UserTable"
---

<objective>
Write unit tests for all public-facing components (freemium-gate, otp-verify-form, phone-verify-form, public-qr-form, public-qr-result-dialog, qr-type-grid) and admin components (user-table, user-qr-table). Completes the test coverage for all custom components.

Purpose: Validates public user flow (phone verification, freemium gate, QR creation) and admin dashboard rendering. Completes Phase 8 component test coverage.
Output: 8 test files covering all public and admin components.
</objective>

<execution_context>
@/Users/enzo.figueiredo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/enzo.figueiredo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-add-unit-tests-to-all-components-and-services/08-CONTEXT.md
@.planning/phases/08-add-unit-tests-to-all-components-and-services/08-RESEARCH.md
@.planning/phases/08-add-unit-tests-to-all-components-and-services/08-01-SUMMARY.md

<interfaces>
<!-- Read source files before writing tests to understand exact props and behavior -->

From src/components/public/freemium-gate.tsx:
```typescript
interface FreemiumGateProps { className?: string }
export function FreemiumGate({ className }: FreemiumGateProps)
// Renders: logo, "You've used your 5 free QR codes" heading, sign-up message, GoogleSignInButton
```

From src/components/public/otp-verify-form.tsx (141 lines):
```typescript
// Uses InputOTP from input-otp library, calls checkOtp and resendOtp Server Actions
// Props include phone, onVerified callback
```

From src/components/public/phone-verify-form.tsx (103 lines):
```typescript
// Phone input form, calls sendOtp Server Action
// Props include onCodeSent callback
```

From src/components/public/public-qr-form.tsx (112 lines):
```typescript
// Similar to QrForm but for public users (no auth)
// Props include action, verifiedPhone, qrType
```

From src/components/public/public-qr-result-dialog.tsx (138 lines):
```typescript
// Shows generated QR code result with download and share options
// Props include qrData (slug, imageUrl, label, platform), open, onOpenChange
```

From src/components/public/qr-type-grid.tsx (52 lines):
```typescript
// Two cards: "Meu QR" and "Custom QR" — public version of QrTypeSelect
// Props include onSelect callback
```

From src/components/admin/user-table.tsx (117 lines):
```typescript
// Table of users with email, QR count, scan count, status
// Props: users array
```

From src/components/admin/user-qr-table.tsx (121 lines):
```typescript
// Table of QR codes for a specific user
// Props: qrCodes array, onToggle callback
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test public components (freemium-gate, otp-verify-form, phone-verify-form, public-qr-form, public-qr-result-dialog, qr-type-grid)</name>
  <files>src/components/public/freemium-gate.test.tsx, src/components/public/otp-verify-form.test.tsx, src/components/public/phone-verify-form.test.tsx, src/components/public/public-qr-form.test.tsx, src/components/public/public-qr-result-dialog.test.tsx, src/components/public/qr-type-grid.test.tsx</files>
  <action>
Read ALL 6 source files first to understand exact prop shapes, imports, and rendering behavior.

1. **FreemiumGate tests** — `src/components/public/freemium-gate.test.tsx`:
   - Mock: `vi.mock('@/app/login/actions', () => ({ signInWithGoogle: vi.fn() }))`
   - Renders "You've used your 5 free QR codes" heading text
   - Renders sign-up prompt text about Google
   - Renders the GoogleSignInButton (check for "Continue with Google" text)
   - Renders FluxQR logo image

2. **OtpVerifyForm tests** — `src/components/public/otp-verify-form.test.tsx`:
   - Read source to find exact Server Action imports and mock them all
   - Mock any Server Actions (checkOtp, resendOtp): `vi.mock('@/app/actions/phone-actions', ...)`  (check actual import path in source)
   - Renders phone number text (the phone prop displayed as context)
   - Renders OTP input area (InputOTP may be tricky in jsdom — if it doesn't render, mock the InputOTP component to render a simple input)
   - Renders "Resend" button or link
   - Note: If InputOTP from 'input-otp' causes issues in jsdom, mock it: `vi.mock('input-otp', () => ({ InputOTP: ({ children, ...props }: any) => <div {...props}>{children}</div>, InputOTPGroup: ({ children }: any) => <div>{children}</div>, InputOTPSlot: () => <input /> }))`

3. **PhoneVerifyForm tests** — `src/components/public/phone-verify-form.test.tsx`:
   - Mock Server Actions used by this component
   - Renders phone number input field
   - Renders submit button (e.g., "Send Code" or "Verify")
   - Shows error message when form action returns an error

4. **PublicQrForm tests** — `src/components/public/public-qr-form.test.tsx`:
   - Mock Server Actions and child component dependencies (slug-input hook, etc.)
   - Renders form fields for QR creation (label, platform, message)
   - Shows verified phone number when verifiedPhone prop provided
   - Hides default message field when qrType='default'
   - Shows default message field when qrType='custom'

5. **PublicQrResultDialog tests** — `src/components/public/public-qr-result-dialog.test.tsx`:
   - When open=true with qrData: renders QR code image, label, slug, "Download" button
   - When open=false: content not in document
   - Renders share button when available

6. **QrTypeGrid tests** — `src/components/public/qr-type-grid.test.tsx`:
   - Renders "Meu QR" card text (public version label — check actual text in source)
   - Renders "Custom QR" card text
   - Calls onSelect('default') when first card clicked
   - Calls onSelect('custom') when second card clicked

**Important patterns for this task:**
- For components that import Server Actions, always mock the action module at file top level
- For components using InputOTP or other complex third-party components, mock them if they fail in jsdom
- Read each source file to find the EXACT import paths before writing mocks

Run: `pnpm vitest run src/components/public/`
  </action>
  <verify>
    <automated>cd /Users/enzo.figueiredo/www/fluxqr && pnpm vitest run src/components/public/ 2>&1 | tail -15</automated>
  </verify>
  <done>All 6 public component test files pass. FreemiumGate validates 5-use limit message. OtpVerifyForm and PhoneVerifyForm test form rendering. PublicQrForm tests create mode with qrType variants. PublicQrResultDialog tests open/closed states. QrTypeGrid tests card selection callbacks.</done>
</task>

<task type="auto">
  <name>Task 2: Test admin components (user-table, user-qr-table) and run full suite</name>
  <files>src/components/admin/user-table.test.tsx, src/components/admin/user-qr-table.test.tsx</files>
  <action>
Read both source files first to understand exact prop shapes, table structure, and action handlers.

1. **UserTable tests** — `src/components/admin/user-table.test.tsx`:
   - Read source to understand the exact props interface (users array shape)
   - Mock any Server Action imports used by the component
   - Renders table headers (Email, QR Codes, Scans, Status, etc.)
   - Renders a row for each user in the array
   - Renders user email in the row
   - Renders QR count for each user
   - Renders user status (active/inactive)
   - Shows empty state or empty table when users array is empty

2. **UserQrTable tests** — `src/components/admin/user-qr-table.test.tsx`:
   - Read source to understand the exact props interface (qrCodes array, onToggle callback)
   - Mock any Server Action imports
   - Renders table headers (Label, Slug, Platform, Scans, Status, etc.)
   - Renders a row for each QR code
   - Renders QR label and slug in rows
   - Renders platform badge
   - Renders scan count
   - Renders toggle/deactivate action button
   - Calls onToggle when toggle action is triggered

3. **Full suite validation** — After writing all tests, run the complete test suite:
   ```
   pnpm test:run --reporter=verbose
   ```
   All tests across all 26 test files (6 from plan 01 + 6 from plan 02 + 6 from plan 03 + 6 from plan 04 + 8 from this plan = 32 test files total... though plans 02-05 run in parallel, the final count should be validated) must pass.

4. **Business rule verification** — If any test reveals a bug or incorrect business logic in the implementation, fix the source code and commit the fix separately before the test commit (per user decision on business rule validation).

Run: `pnpm vitest run src/components/admin/`
Final: `pnpm test:run --reporter=verbose`
  </action>
  <verify>
    <automated>cd /Users/enzo.figueiredo/www/fluxqr && pnpm vitest run src/components/admin/ 2>&1 | tail -15</automated>
  </verify>
  <done>UserTable renders user rows with email, QR count, status. UserQrTable renders QR details with toggle action. Full test suite passes across all test files in the project.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run src/components/public/ src/components/admin/` passes all 8 test files
- `pnpm test:run` passes the entire project test suite (all plans combined)
- No source code bugs found (or if found, fixed inline with separate commits)
</verification>

<success_criteria>
- All 6 public component test files pass
- Both admin component test files pass
- FreemiumGate validates the 5-use limit message
- Phone verification flow components tested for form rendering
- Admin tables tested for data rendering and action callbacks
- Full project test suite is green
</success_criteria>

<output>
After completion, create `.planning/phases/08-add-unit-tests-to-all-components-and-services/08-05-SUMMARY.md`
</output>
