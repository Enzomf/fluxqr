---
phase: 02-scanner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/redirect.ts
  - src/lib/__tests__/redirect.test.ts
  - src/components/scanner/telegram-fallback.tsx
  - src/app/q/[slug]/scanner-landing.tsx
autonomous: true
requirements:
  - SCAN-03
  - SCAN-04
  - SCAN-05

must_haves:
  truths:
    - "buildPlatformUrl returns correct WhatsApp deep link with encoded message"
    - "buildPlatformUrl returns correct SMS deep link with encoded message"
    - "buildPlatformUrl returns Telegram link without message parameter"
    - "buildPlatformUrl strips non-digit characters from WhatsApp contact target"
    - "Scanner landing renders label, editable textarea pre-filled with default message, and platform CTA"
    - "CTA button is disabled when textarea is empty"
    - "Textarea has 500 character hard limit"
    - "Telegram fallback shows copy button and open button with hint text"
    - "Copy button changes to checkmark confirmation for 2 seconds"
  artifacts:
    - path: "src/lib/redirect.ts"
      provides: "Deep link URL builder for WhatsApp, SMS, Telegram"
      exports: ["buildPlatformUrl"]
    - path: "src/lib/__tests__/redirect.test.ts"
      provides: "Unit tests for buildPlatformUrl"
      min_lines: 20
    - path: "src/components/scanner/telegram-fallback.tsx"
      provides: "Telegram copy+open two-button fallback component"
      exports: ["TelegramFallback"]
    - path: "src/app/q/[slug]/scanner-landing.tsx"
      provides: "Interactive scanner landing with textarea and platform CTA"
      exports: ["ScannerLanding"]
  key_links:
    - from: "src/app/q/[slug]/scanner-landing.tsx"
      to: "src/lib/redirect.ts"
      via: "import buildPlatformUrl"
      pattern: "import.*buildPlatformUrl.*from.*@/lib/redirect"
    - from: "src/app/q/[slug]/scanner-landing.tsx"
      to: "src/components/scanner/telegram-fallback.tsx"
      via: "renders TelegramFallback for telegram platform"
      pattern: "<TelegramFallback"
---

<objective>
Build the client-side scanner landing components and deep link URL builder.

Purpose: Create all interactive pieces that scanner users interact with -- the message textarea, platform CTA buttons, Telegram copy fallback, and the pure function that constructs deep link URLs. These are the building blocks that the server-side page.tsx (Plan 02) will render.

Output: `redirect.ts` utility with tests, `scanner-landing.tsx` client component, `telegram-fallback.tsx` client component.
</objective>

<execution_context>
@/Users/enzo.figueiredo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/enzo.figueiredo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scanner/02-CONTEXT.md
@.planning/phases/02-scanner/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types/index.ts:
```typescript
export type Platform = 'whatsapp' | 'sms' | 'telegram'

export type QrCode = {
  id: string
  user_id: string
  slug: string
  label: string
  platform: Platform
  contact_target: string
  default_message: string | null
  is_active: boolean
  scan_count: number
  created_at: string
  updated_at: string
}
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```

From src/components/ui/button.tsx:
```typescript
// shadcn Button component -- use variant="outline" for ghost-style, standard for primary
// Supports: variant, size, disabled, className, onClick, children
```

From src/app/login/page.tsx (centered card layout pattern to match):
```tsx
<main className="min-h-screen flex items-center justify-center bg-surface">
  <div className="bg-surface-raised rounded-lg p-8 w-full max-w-sm">
    {/* card content */}
  </div>
</main>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build deep link URL utility with tests</name>
  <files>src/lib/redirect.ts, src/lib/__tests__/redirect.test.ts</files>
  <behavior>
    - buildPlatformUrl('whatsapp', '5511999998888', 'Hello') returns 'https://wa.me/5511999998888?text=Hello'
    - buildPlatformUrl('whatsapp', '+55 11 99999-8888', 'Hello') strips non-digits: returns 'https://wa.me/5511999998888?text=Hello'
    - buildPlatformUrl('sms', '5511999998888', 'Hello') returns 'sms:5511999998888?body=Hello'
    - buildPlatformUrl('telegram', 'myusername', 'Hello') returns 'https://t.me/myusername' (no message param)
    - Message with special characters (emoji, newlines, ampersands) is properly URI-encoded for WhatsApp and SMS
    - Empty message for WhatsApp still includes ?text= with empty encoded string
  </behavior>
  <action>
    RED phase: Create `src/lib/__tests__/redirect.test.ts` with tests covering all behaviors above. Run tests -- they must fail (module not found).

    GREEN phase: Create `src/lib/redirect.ts` exporting `buildPlatformUrl(platform: Platform, contactTarget: string, message: string): string`. Implementation:
    - Import `Platform` from `@/types`
    - For WhatsApp: strip non-digits from contactTarget (`contactTarget.replace(/\D/g, '')`), return `https://wa.me/${cleaned}?text=${encodeURIComponent(message)}`
    - For SMS: return `sms:${contactTarget}?body=${encodeURIComponent(message)}`
    - For Telegram: return `https://t.me/${contactTarget}` (no message -- Telegram deep links do not support pre-fill)
    - Use a switch statement with exhaustive cases matching the Platform union type

    Also export two helper functions used by scanner-landing.tsx:
    - `platformColor(platform: Platform): string` -- returns '#25D366' (WhatsApp), '#0088CC' (Telegram), '#6366F1' (SMS/brand-500 indigo)
    - `platformLabel(platform: Platform): string` -- returns 'Abrir WhatsApp', 'Abrir Telegram', 'Enviar SMS'

    Run tests -- they must pass.

    Note: Use vitest if available, otherwise use jest. Check package.json for test runner. If neither is configured, use a simple Node.js test with `node --test` or skip automated tests and verify by type-checking (`pnpm tsc --noEmit`).
  </action>
  <verify>
    <automated>cd /Users/enzo.figueiredo/www/fluxqr && pnpm tsc --noEmit</automated>
  </verify>
  <done>
    - `buildPlatformUrl` correctly constructs WhatsApp, SMS, and Telegram deep link URLs
    - WhatsApp contact target has non-digit characters stripped
    - Messages are URI-encoded with `encodeURIComponent`
    - Telegram URL contains no message parameter
    - `platformColor` and `platformLabel` helpers exported
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Telegram fallback component</name>
  <files>src/components/scanner/telegram-fallback.tsx</files>
  <action>
    Create `src/components/scanner/telegram-fallback.tsx` as a `"use client"` component.

    **Props interface:**
    ```typescript
    interface TelegramFallbackProps {
      message: string
      contactTarget: string
      onMessageChange: (msg: string) => void
      label: string
    }
    ```

    **Implementation:**
    - Export named `TelegramFallback` component
    - Local state: `copied` boolean (useState, default false)
    - Renders the same centered card layout as login page: `<main className="min-h-screen flex items-center justify-center bg-surface">` wrapping a `<div className="bg-surface-raised rounded-lg p-8 w-full max-w-sm">`
    - Inside the card:
      1. `<h1>` with `label` text -- `text-foreground font-semibold text-lg`
      2. `<textarea>` -- controlled by `message`/`onMessageChange`, `maxLength={500}`, `rows={4}`, placeholder `"Digite sua mensagem..."`, resize-none, styled with `bg-surface-overlay text-foreground p-3 text-sm rounded-md border border-border focus:outline-none focus:ring-2 focus:ring-ring`
      3. Hint paragraph: `"O Telegram nao suporta mensagens pre-preenchidas. Copie e cole no chat."` -- `text-xs text-muted-foreground mt-3`. Use the exact Portuguese text from CONTEXT.md with proper diacritics: "O Telegram nao suporta mensagens pre-preenchidas. Copie e cole no chat."
      4. Two stacked buttons in a `flex flex-col gap-2 mt-4` container:
         - **Copy button:** shadcn `<Button variant="outline">` -- onClick calls `navigator.clipboard.writeText(message)`, sets `copied=true`, `setTimeout` resets to false after 2000ms. When `copied=true`, show `<Check className="mr-2 h-4 w-4" />` icon from lucide-react + "Mensagem copiada!". When `copied=false`, show "Copiar mensagem". Disabled when `!message.trim()`.
         - **Open Telegram button:** shadcn `<Button>` with inline style `backgroundColor: '#0088CC'` and `color: '#fff'`. onClick sets `window.location.href = \`https://t.me/${contactTarget}\``. Text: "Abrir Telegram".

    **Imports:** `useState` from react, `Check` from lucide-react, `Button` from `@/components/ui/button`, `cn` from `@/lib/utils`.

    Keep the component lean -- no unnecessary imports. This component is part of the scanner page's JS budget (under 10KB total).
  </action>
  <verify>
    <automated>cd /Users/enzo.figueiredo/www/fluxqr && pnpm tsc --noEmit</automated>
  </verify>
  <done>
    - TelegramFallback component renders centered card with label, textarea, hint, and two buttons
    - Copy button uses navigator.clipboard.writeText and shows 2-second confirmation state
    - Open Telegram button navigates to t.me/{contactTarget}
    - Copy button disabled when message is empty
    - Component uses "use client" directive
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Build scanner landing client component</name>
  <files>src/app/q/[slug]/scanner-landing.tsx</files>
  <action>
    Create `src/app/q/[slug]/scanner-landing.tsx` as a `"use client"` component.

    **Props:** `{ qr: QrCode }` -- receives the full QR record from the server component.

    **Implementation:**
    - Export named `ScannerLanding` component
    - Local state: `message` string (useState, initialized to `qr.default_message ?? ''`)
    - **Telegram platform:** If `qr.platform === 'telegram'`, render `<TelegramFallback>` passing `message`, `contactTarget={qr.contact_target}`, `onMessageChange={setMessage}`, `label={qr.label}`. Return early.
    - **WhatsApp/SMS platforms:** Render centered card layout (same as login page pattern):
      - `<main className="min-h-screen flex items-center justify-center bg-surface">`
      - `<div className="bg-surface-raised rounded-lg p-8 w-full max-w-sm">`
      - Inside the card:
        1. `<h1>` with `qr.label` -- `text-foreground font-semibold text-lg`
        2. `<textarea>` -- controlled by `message`/`setMessage`, `maxLength={500}`, `rows={4}`, placeholder `"Digite sua mensagem..."`, resize-none, same styling as TelegramFallback textarea
        3. Character count display: `<p className="mt-1 text-xs text-muted-foreground text-right">{message.length}/500</p>`
        4. Platform CTA `<Button>` -- full width (`w-full mt-4`), `disabled={!message.trim()}`, onClick builds URL via `buildPlatformUrl(qr.platform, qr.contact_target, message)` and sets `window.location.href` to it.
           - Button style: inline `backgroundColor` set to `platformColor(qr.platform)`, `color: '#fff'`
           - Button content: platform icon from lucide-react + `platformLabel(qr.platform)`
           - WhatsApp icon: `<MessageCircle className="mr-2 h-5 w-5" />`
           - SMS icon: `<MessageSquare className="mr-2 h-5 w-5" />`

    **Imports:** `useState` from react, `MessageCircle`, `MessageSquare` from lucide-react, `Button` from `@/components/ui/button`, `buildPlatformUrl`, `platformColor`, `platformLabel` from `@/lib/redirect`, `TelegramFallback` from `@/components/scanner/telegram-fallback`, `QrCode` type from `@/types`.

    Keep the component lean for the 10KB JS budget. No unnecessary shadcn components or heavy libraries.
  </action>
  <verify>
    <automated>cd /Users/enzo.figueiredo/www/fluxqr && pnpm tsc --noEmit</automated>
  </verify>
  <done>
    - ScannerLanding renders different UI for telegram vs whatsapp/sms
    - Telegram delegates to TelegramFallback component
    - WhatsApp/SMS show textarea + colored CTA button with platform icon and label
    - CTA disabled when message is empty
    - Textarea has 500 char limit and character count display
    - Clicking CTA navigates to correct deep link URL via buildPlatformUrl
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` passes with zero errors
- `src/lib/redirect.ts` exports `buildPlatformUrl`, `platformColor`, `platformLabel`
- `src/components/scanner/telegram-fallback.tsx` exports `TelegramFallback`
- `src/app/q/[slug]/scanner-landing.tsx` exports `ScannerLanding`
- All components use "use client" directive
- No Supabase queries in any client component
- No Framer Motion imports
</verification>

<success_criteria>
- Deep link URL builder correctly constructs WhatsApp, SMS, and Telegram URLs
- Scanner landing component renders editable textarea with platform-specific CTA
- Telegram fallback provides copy-to-clipboard + open Telegram two-button flow
- All three files compile cleanly with TypeScript strict mode
- Components follow established patterns: cn() for class merging, shadcn Button, dark theme tokens
</success_criteria>

<output>
After completion, create `.planning/phases/02-scanner/02-01-SUMMARY.md`
</output>
